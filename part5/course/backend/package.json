{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "license": "MIT",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "build:ui": "rm -rf dist && cd ../frontend && npm install && npm run build && cp -r dist ../backend",
    "dev": "NODE_ENV=development node --watch index.js",
    "fullstack": "npm run build:ui && npm run dev",
    "test": "NODE_ENV=test node --test",
    "lint": "eslint .",
    "start:test": "cross-env NODE_ENV=test node --watch index.js"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.9.4"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.10.0",
    "cross-env": "^10.1.0",
    "eslint": "^10.9.1",
    "globals": "^17.12.0",
    "supertest": "^7.2.2"
  }
}
